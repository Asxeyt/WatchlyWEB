# KategoriSecici

ASP.NET Core MVC + SQLite proje.

## Local Run

```powershell
dotnet run
```

## Docker Run

```powershell
docker build -t kategorisecici .
docker run -p 8080:8080 kategorisecici
```
